{
  "name": "dropReplacer",
  "version": "0.0.1",
  "description": "rename drag-dropped files by regex.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . --platform=win32 --arch=ia32,x64 --out=build"
  },
  "author": "",
  "license": "BSD-2-Clause",
  "devDependencies": {
    "electron-packager": "^8.0.0",
    "electron-prebuilt": "^1.3.5"
  }
}
